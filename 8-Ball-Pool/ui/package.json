{
  "name": "dart-pool-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "validate-physics": "tsx ../scripts/validate-physics.ts",
    "verify": "npm run typecheck && npm run test && npm run validate-physics"
  },
  "dependencies": {
    "phaser": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vite": "^6.4.0",
    "vitest": "^2.1.0"
  }
}
